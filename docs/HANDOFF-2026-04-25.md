# Handoff — Sesión 2026-04-25

> Sesión larga del 25 de abril. Backend ahora corriendo end-to-end contra el 3CX productivo de pruebas. Parser de logs arreglado y validado. Falta solo SIPp para hacer pruebas activas reales.

---

## TL;DR

- ✅ **VPN Fortinet** validada — IP `192.168.254.10` lado OLAM.
- ✅ **SSH key auth** al 3CX funcionando (Opción B implementada — sin password en `.env`).
- ✅ **node_exporter** activado en el 3CX, métricas de host vía **SSH tunnel** (firewall upstream bloquea :9100 directo).
- ✅ **Regex de host** arreglados — `disk.os` y `network.rx/tx` ahora reflejan datos reales.
- ✅ **Parser de logs arreglado y validado** — errors408/503 cuentan correctamente, los falsos positivos del regex viejo eliminados.
- ✅ **Postman collection** completa con tests automáticos (creada el 24).
- ✅ **Mock vs real** validado — mock no toca el 3CX, real sí.
- ❌ **SIPp** no instalado en la máquina del backend — bloquea el modo activo.
- ❌ **WSL** no disponible — `wsl --install` requiere admin + reboot, no autónomo.

**Estado al cierre:** backend corriendo en `:3000` PRODUCTION MODE, frontend en `:5173`. Dashboard mostrando datos reales del 3CX productivo de pruebas.

---

## Lo que se hizo (orden cronológico)

### 1. SSH al 3CX — Opción B (key-based)

```bash
ssh-keygen -t ed25519 -f backend/keys/3cx_rsa -N ""
# pubkey instalada manualmente en authorized_keys del 3CX vía sesión SSH abierta
ssh -i backend/keys/3cx_rsa root@172.18.164.28 "hostname"   # ✅
```

**Trampa encontrada:** `sshpass` de Cygwin/Git Bash falló con el password `Olam2026$` (el `$` se interpreta raro). Solución: pegar la pubkey manualmente en una sesión SSH ya autenticada del usuario.

### 2. node_exporter + SSH tunnel para Prometheus

**Lo encontrado:**
- node_exporter v1.5.0 ya estaba instalado en el 3CX, solo no estaba enabled.
- `apt install -y prometheus-node-exporter && systemctl enable --now prometheus-node-exporter` lo activó.
- ✅ Servicio escuchando en `*:9100`.

**Sorpresa:** firewall **upstream** (router OLAM o policy Fortinet) bloquea el puerto desde la subnet VPN. Confirmado con tcpdump en el 3CX: SYN packets nunca llegan al server. El firewall del 3CX (nftables) tiene `tcp dport 9100 accept` correctamente.

**Solución aplicada:** SSH tunnel en [sshClient.js](../backend/src/services/sshClient.js) — `forwardOut('127.0.0.1', 0, '127.0.0.1', 9100, ...)`. El backend pega a `http://127.0.0.1:9100` y el túnel forwardea por la conexión SSH ya existente.

**Detalle técnico:** los listeners de `error` deben estar **antes** de `socket.pipe(stream).pipe(socket)` — sino un `ECONNRESET` en cualquiera de las puntas tira el proceso. Aprendido a la mala (crash de backend hace unas horas).

**Cuando OLAM abra el puerto upstream:** poner `NODE_EXPORTER_VIA_SSH=false` en `.env` y `NODE_EXPORTER_URL=http://172.18.164.28:9100/metrics`. El código sigue siendo válido.

### 3. Regex de host arreglados ([metricsCollector.js](../backend/src/services/metricsCollector.js))

**Problema:** las queries buscaban claves exactas que no existen.

**Antes:**
```js
pm['node_filesystem_size_bytes{mountpoint="/"}']  // undefined
pm['node_network_receive_bytes_total{device="eth0"}']  // undefined
```

**La realidad del 3CX:**
- Filesystem: `node_filesystem_size_bytes{device="/dev/sda1",fstype="ext4",mountpoint="/"}` (392G total, 4.6G usado).
- Network: `device="ens192"` y `device="ens224"` — VMware, **no** `eth0`.
- No hay mount separado para grabaciones; viven dentro de `/`.

**Después:**
```js
findByLabel(pm, 'node_filesystem_size_bytes', k => k.includes('mountpoint="/"'))
sumByLabel(pm, 'node_network_receive_bytes_total', PHYSICAL_IFACE)  // suma todas las físicas
```

`disk.recordings` queda hardcoded en 0 porque no hay mount separado — pendiente cuando ops del cliente cree uno.

### 4. Parser de logs — el bloqueo más grande del día

**Problemas encontrados:**

1. **Buffering de stdout** — `tail -F` con stdout pipeado por SSH bufferiza por bloques (4KB+). Líneas individuales se quedan en buffer indefinidamente. **Fix:** prepend `stdbuf -oL` al comando.
2. **Flag `-q`** — `tail -Fq` SUPRIME los headers `==> file <==`. Pero `detectFile()` los necesita para saber qué archivo es cada línea. **Fix:** quitar `-q`. Ahora el tail emite headers, el parser asigna el archivo correctamente.
3. **`-n 0`** — sin esto, cada arranque del backend dumpeaba todo el histórico de los 5 logs (MB de datos). Agregado para evitar.
4. **Regex de timestamp inválido** — esperaba `[DD/MM/YYYY HH:MM:SS.mmm]` con corchetes. El formato real:
   - 4 logs: `YYYY/MM/DD HH:MM:SS.mmm|####|Level|...` (CallFlow, Gateway, Queue, System).
   - IVR: `DD/MM/YYYY HH:MM:SS.mmm [hex] ...`.
   - **Ningún log usa corchetes**. **Fix:** dos regex (`TS_YMD_RE`, `TS_DMY_RE`).
5. **Falso positivo de 408/503** — regex `/408/` matcheaba cualquier "408" en cualquier parte (incluyendo timestamps tipo `04:08`). **Fix:** regex estricto exigiendo contexto SIP: `\b(SIP\/2\.0\s+408|408\s+Request\s+Timeout|status[:=]\s*408)\b`.
6. **SystemService — abreviación 3CX** — el log usa `Erro` (sin la `r` final), no `Error`. **Fix:** detectar nivel en formato pipe-separated `\|(Erro|Crit|Fatal|Warn)\|`.
7. **CallFlow regex inventados** — los ejemplos del comentario nunca aparecen en logs reales. Sin tráfico SIPp no podemos validar el formato exacto. **Fix temporal:** patrones defensivos basados en docs 3CX (INVITE, BYE, CANCEL, ParentConnectionTerminated, Released).
8. **State con Set sin callId estable** — `state.activeCalls = new Set()` requería un callId para hacer add/delete. Como las líneas reales no tienen callId estable, `delete(undefined)` no hacía nada. **Fix:** counter incremental con piso en 0.

**Validación end-to-end del fix:**

Inyecté 6 líneas SIP/2.0 408 vía SSH:
```
==> /var/lib/3cxpbx/Instance1/Data/Logs/3CXGatewayService.log <==
2026/04/25 15:10:00.000|9999|Info| SIP/2.0 408 single line test
2026/04/25 15:11:00.000|9999|Info| SIP/2.0 408 batch 1
... (4 más)
```

Resultado del API: `{"errors408": 6, "errors503": 0}` ✅

**Latencia observada:** ~3-5s entre escritura del log y conteo en backend. Es por buffering del SSH stream (poll de tail cada 1s + agrupamiento del canal SSH). Aceptable para dashboard de monitoreo.

### 5. SIPp install — bloqueado

**Camino intentado:** WSL Ubuntu → `apt install sip-tester`.

**Problema:** WSL no está instalado en la máquina. `wsl --install` requiere:
- Permisos de administrador
- Reboot post-instalación (segunda fase del install)

**No se hizo autónomo** porque era riesgoso sin supervisión humana.

**Alternativas evaluadas:**
- ❌ Binario Windows oficial — SIPp NO publica binario Windows oficial; la gente compila desde source con MinGW.
- ⚠️ Binario Windows no-oficial (forks GitHub) — riesgo de seguridad, descartado.
- ❌ Correr SIPp en el 3CX vía SSH — viola la regla de [CLAUDE.md](../CLAUDE.md): "SIPp NUNCA en el 3CX, siempre en el host del backend, o las métricas se contaminan".
- ⚠️ Correr SIPp en el equipo Windows `172.18.164.35` (RDP) — requiere reescribir el SIPp Manager para SSH/RDP a otro host. ~2-4 horas de código.

---

## Cómo levantar el stack ahora

### Pre-requisitos
- VPN Fortinet conectada (IP `192.168.254.10` o similar lado OLAM).
- `Test-NetConnection 172.18.164.28 -Port 22 → True`.

### Backend
```powershell
cd "C:\Users\Maximiliano Pulido\OLAM\backend"
npm start
```

Salida esperada:
```
[DB] Initialized
[SSH] Connected to 172.18.164.28
[OLAM] Backend running on http://localhost:3000 [PRODUCTION MODE]
[SSH tunnel] Forwarding 127.0.0.1:9100 → 3CX:9100
```

### Frontend
```powershell
cd "C:\Users\Maximiliano Pulido\OLAM\frontend"
npm run dev
```

Abrir `http://localhost:5173` (o `:5174` si `:5173` está ocupado).

### Validar que todo funciona
```bash
curl http://localhost:3000/api/health
# Esperado: {"status":"ok","mock":false,"ssh":true,...}

curl http://localhost:3000/api/status/host
# Esperado: cpu/ram/disk/network con valores != 0
```

---

## Estado de archivos modificados (sin commitear)

```
backend/.env                                # creado, gitignored
backend/keys/3cx_rsa                        # par de claves, gitignored
backend/keys/3cx_rsa.pub                    # gitignored
backend/src/services/sshClient.js           # SSH tunnel
backend/src/services/metricsCollector.js    # regex disk/network
backend/src/services/logReader.js           # tail command, counter activeCalls
backend/src/services/logParser.js           # timestamp + 408/503 + Erro pipe-format
docs/HANDOFF-2026-04-24.md                  # con insurance update
docs/HANDOFF-2026-04-25.md                  # este archivo
postman/                                    # creado el 24
```

Pre-existentes con cambios del usuario (no tocados hoy):
- `backend/src/db/queries.js`, `schema.js`, `routes/history.js`, `services/sippManager.js`, `package.json`

---

## Próximos pasos — orden recomendado

### Inmediato — instalar SIPp para destrabe modo activo

```powershell
# 1. Instalar WSL2 + Ubuntu (requiere admin + reboot)
wsl --install
# Después del reboot, configurar usuario Ubuntu, después:
wsl
sudo apt update && sudo apt install -y sip-tester
sipp -v   # confirma versión 3.x
exit
```

Modificar [sippManager.js:136](../backend/src/services/sippManager.js#L136):
```js
const sippBin = os.platform() === 'win32' ? 'wsl' : 'sipp';
const args = os.platform() === 'win32'
  ? ['sipp', target, '-sn', 'uac', /* resto ... */]
  : [target, '-sn', 'uac', /* ... */];
```

### Antes de la primera prueba SIPp real
- **Sanitizar `destination`** ([sippManager.js:142](../backend/src/services/sippManager.js#L142)) — bug de inyección heredado.
- **Confirmar destino seguro** — el default es ext `100`. Validar si esa extensión:
  - Existe en el 3CX
  - No es de un agente real
  - Idealmente, crear una ext de prueba dedicada en el 3CX (vía web UI con user `1910`).
- **Autorizar IP del backend como peer SIP** en el 3CX, si los INVITE son rechazados con 403.

### Validación con tcpdump (para demostrar real vs mock)
```bash
# En el 3CX, antes del smoke test:
ssh -i backend/keys/3cx_rsa root@172.18.164.28 \
  "tcpdump -i any -n 'udp port 5060 and src host 192.168.254.10' -c 50"

# Disparar smoke desde Postman/UI
# tcpdump tiene que mostrar SYN/INVITE desde 192.168.254.10
```

### Bugs pendientes (no bloqueantes)

1. **Encoding UTF-8 → Latin-1 en alertas** — los mensajes salen como `Ã¡` en el JSON. Investigar dónde se serializan las alertas Fase 0.
2. **CallFlow regex sin validar con tráfico real** — las regex actuales son defensivas. Cuando se haga el primer SIPp test, capturar líneas reales de `3CXCallFlow.log` y refinar.
3. **Watchdog del LogReader spammea console.warn** cada minuto cuando hay baja actividad — no afecta funcionalidad pero llena el log. Cambiar a alerta única + flag.
4. **`disk.recordings` siempre 0** — hasta que ops monte un volumen separado para grabaciones.
5. **`destination` no sanitizado** — bug de inyección de shell.

---

## Detalles técnicos para próxima sesión

### Comando final del log reader
```js
const cmd = `stdbuf -oL tail -F -n 0 ${LOG_FILES.join(' ')} 2>&1`;
```

- `stdbuf -oL`: line-buffered (sino bloques 4KB).
- `tail -F`: follow + manejar rotaciones por nombre de archivo.
- **Sin `-q`**: los headers `==> file <==` SON necesarios para detectar archivo.
- `-n 0`: solo seguir lo nuevo, no dumpear histórico.

### SSH tunnel — patrón
```js
ssh.connection.forwardOut(
  '127.0.0.1', 0,
  '127.0.0.1', TUNNEL_REMOTE_PORT,
  (err, stream) => {
    if (err) { socket.destroy(); return; }
    // Listeners ANTES del pipe, sino ECONNRESET tira el proceso
    socket.on('error', () => stream.destroy());
    stream.on('error', () => socket.destroy());
    socket.pipe(stream).pipe(socket);
  },
);
```

### Formato de logs del 3CX v20

| Log | Formato |
|---|---|
| 3CXCallFlow.log | `YYYY/MM/DD HH:MM:SS.mmm\|####\|Level\| message` |
| 3CXGatewayService.log | idem |
| 3CXQueueManager.log | idem |
| 3cxSystemService.log | idem |
| 3CXIVR.log | `DD/MM/YYYY HH:MM:SS.mmm [hex_id] message` |

Niveles: `Info`, `Warn`, `Erro` (sin la `r` final), `Crit`, `Fatal`.

---

## Hallazgos Fase 0 — siguen activos

| ID | Severidad | Descripción |
|---|---|---|
| H-01 | CRÍTICO | Licencia SC32 insuficiente. >32 calls rechazadas por el 3CX. |
| H-07 | CRÍTICO | SIP sin TLS en IP pública `181.63.161.242`. Riesgo toll fraud. |
| H-03 | ALTO | Errores 408 en troncal Tigo UNE. |
| H-05 | ALTO | Auto-updates 3CX habilitado. Riesgo de reinicio + cambio de formato de logs. |
