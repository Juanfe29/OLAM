# Estado del proyecto OLAM 3CX Audit — 2026-04-30

> Snapshot de inventario y estado al cierre de sesión 2026-04-30 (post-Fase 6 parcial).

---

## Inventario de herramientas

### Mi laptop dev (Maximiliano Pulido)

| Item | Ubicación |
|---|---|
| Repo OLAM (con `.git`) | `c:\Users\Maximiliano Pulido\OLAM` |
| Bundle Cygwin para re-deploy | `C:\Users\Maximiliano Pulido\cygwin-bundle\` (~180 MB) y `cygwin-bundle.zip` (183 MB) |
| Remote git | `https://github.com/Juanfe29/OLAM.git` (origin/main al día) |

### RDP Win10 — `172.18.164.35` (donde vive la app)

| Item | Estado | Ubicación |
|---|---|---|
| Node.js 20.18.0 + npm 10.8.2 | ✅ | system PATH |
| Backend OLAM | ✅ corriendo `:3001` PRODUCTION MODE | `C:\Users\lamda\OLAM\backend\` |
| Frontend OLAM | ❓ probablemente `:5173` (no revalidado hoy) | `C:\Users\lamda\OLAM\frontend\` |
| Cygwin per-user (1.5 GB) | ✅ instalado | `C:\Users\lamda\cygwin64\` |
| SIPp v3.7.3 binary | ✅ compilado y validado | `C:\Users\lamda\cygwin64\home\lamda\sipp-3.7.3\sipp.exe` |
| Clave SSH al 3CX | ✅ key-based, passwordless | `C:\Users\lamda\OLAM\backend\keys\3cx_rsa` |
| `.env` configurado | ✅ con `SIPP_BIN` + `SIPP_AUTH_USER=1910` + `SIPP_AUTH_PASS` | `C:\Users\lamda\OLAM\backend\.env` |
| `cygwin64\bin` en user PATH | ✅ persistente | HKCU |
| Repo como `.git` | ❌ no es repo, archivos copiados manual desde `\\tsclient` | — |

### 3CX server — `172.18.164.28` (lo que auditamos)

| Item | Estado |
|---|---|
| Debian 12 + 3CX v20 Update 8 Build 1121 | ✅ |
| Licencia SC32 (objetivo SC192) | ⚠️ hallazgo crítico H-01 |
| `node_exporter` en `:9100` | ✅ instalado (solo para tests grandes) |
| Logs en `/var/lib/3cxpbx/Instance1/Data/Logs/` | ✅ los 5 archivos accesibles |
| Troncal SIP Tigo UNE `172.17.179.166:5060` | ✅ registrada (con errores 408 — H-03) |
| SSH passwordless desde RDP | ✅ |
| 3CX Web Console | `https://regis2026.3cx.co/#/login` (creds en HANDOFF-2026-04-27.md) |

---

## Estado del proyecto

### Always-on (corriendo 24/7 en `.35`)

| Capa | Estado |
|---|---|
| Backend Express + WebSocket `:3001` | ✅ PRODUCTION MODE |
| SSH persistente al 3CX | ✅ Conectado |
| Parser de logs del 3CX | ⚠️ corriendo pero con warning `[LogReader] No log data for 2+ minutes` que no diagnosticamos aún |
| Anomaly detector | ✅ activo |
| 4 alertas Fase 0 (H-01 H-03 H-05 H-07) | ✅ visibles en dashboard |
| Frontend dashboard | ❓ no revalidado hoy (estaba up el 27/04) |

### Test-time (on-demand)

| Capa | Estado |
|---|---|
| Backend invoca `sipp.exe` con `SIPP_BIN` | ✅ commit `4efd49b` |
| Backend pasa `-au/-ap` cuando hay creds | ✅ commit `ac11d81` |
| SIPp se autentica contra el 3CX (digest auth) | ✅ confirmado por log (no más `407` en errors.log) |
| Smoke test (1 call, dest=100) | ⚠️ `result: FAIL` con `snapshots: []`. Probable bug de medición del backend (parser de stderr vacío con `stdio:pipe`), no de la llamada. **Pendiente confirmar con tcpdump/3CX log.** |
| Tests grandes (light / medium / peak / soak) | ⏳ esperan que smoke pase clean |
| `node_exporter` scraping | ⏳ se enciende cuando arrancamos tests grandes |
| `tcpdump`/`sngrep` en el 3CX | 🔧 manual vía SSH cuando sea necesario |

---

## Pendientes

### Inmediato (bloquea tests reales)

- [ ] Diagnosticar el FAIL del smoke. Pendientes 2 outputs:
  - Archivos `*_errors.log` / `*_statistics.csv` modificados hoy en `C:\Users\lamda\` (SIPp puede haberlos escrito en otro cwd)
  - `3CXGatewayService.log` del 3CX al momento del test (~16:39 hora local)
- [ ] Cambiar `destination` del smoke a una ext que sí exista en este 3CX (probablemente `100` no existe — confirmar via 3CX Management Console o web client `1910`)
- [ ] Investigar warning del parser always-on (`No log data for 2+ minutes`)

### Mediano plazo (para reporte de assessment)

- [ ] Implementar Opción B: scenario XML custom de SIPp para tests grandes (mejor escalabilidad >50 calls; ver `docs/PLAN-Cygwin-SIPp.md` Fase 6 opción B)
- [ ] Mejorar `buildSummary` del backend para leer el `_statistics.csv` final en lugar de parsear stderr
- [ ] Conseguir licencia trial SC192 o ventana al límite contractual para probar peak (180 calls)

### Operativo

- [ ] Push a GitHub personal (a medias en `docs/HANDOFF-2026-04-27.md`)
- [ ] Convertir el repo del RDP en un `.git` repo (`git init` + remote + cherry-pick) para evitar tener que copiar archivos manualmente

### Bugs conocidos no bloqueantes

- [ ] Encoding UTF-8 → Latin-1 en alertas (`Ã¡` en vez de `á`)
- [ ] `disk.recordings` siempre en 0 (no hay mount separado en el 3CX)
- [ ] `destination` no sanitizado en SIPp Manager (heredado)
- [ ] Watchdog del `LogReader` spammea `console.warn` en baja actividad

---

## Resumen ejecutivo (para el cliente / reporte)

**Modo pasivo (siempre on):** ✅ Operativo. Datos reales del 3CX visibles en el dashboard. 4 alertas Fase 0 detectadas y mostrándose desde el día uno.

**Modo activo (SIPp synthetic load):**
- Stack técnico al 100% — Cygwin instalado, SIPp v3.7.3 compilado, backend wired-up, digest auth funcionando.
- Único bloqueador para arrancar tests serios: el destination default (`100`) probablemente no existe en este 3CX. Cambio trivial cuando confirmemos extensiones válidas.
- Para tests >50 calls falta migrar a scenarios XML custom (1-2hs de trabajo).
- Para tests al límite (180 calls) hace falta upgrade temporal de licencia SC32 → SC192.

**Próximo entregable bloqueado por:**
- Ext válida para destination del smoke (cliente)
- Licencia trial SC192 (cliente)

---

## Refs

- Plan ejecutado: [PLAN-Cygwin-SIPp.md](./PLAN-Cygwin-SIPp.md)
- Handoff sesión 2 (deploy): [HANDOFF-2026-04-27.md](./HANDOFF-2026-04-27.md)
- Handoff sesión 3 (Cygwin/SIPp): [HANDOFF-2026-04-27-cygwin.md](./HANDOFF-2026-04-27-cygwin.md)
- Setup completo del deployment: [HANDOFF-2026-04-25.md](./HANDOFF-2026-04-25.md)
